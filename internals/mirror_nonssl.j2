server {
  listen 80;
  listen [::]:80;
  server_name {{ domain }};

  location '/' {
    return 302 https://$server_name$request_uri;
  }

  location '/.well-known/' {
    root /tmp/letsencrypt;
    default_type "text/plain";
  }

}
